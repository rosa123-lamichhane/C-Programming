/* Title:WAP_IN _C_to_check_if the _given_characters_is_alphabet,_no_or_symbol.c
   name:
   Date:
   */
   #include<stdio.h>//preprocessor directive
   int main()//entry point in c
   {
    char character;//initialize
    printf(Enter the Character:);
    scanf("%c%c%",&alphabeet,&no,&symbol);
    if(alphabets=a>z)
    {
       printf("alphabet");
    }
    else if()
    {
        printf("no.");
    }
    else()
    {
        printf("Symbol");
    }



       return 0;
   }
